apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-postgres-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio-postgres
  template:
    metadata:
      labels:
        app: portfolio-postgres
    spec:
      containers:
        - name: portfolio-postgres
          image: postgres
          env:
            - name: POSTGRES_DB
              value: portfolio
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-portfolio-srv
spec:
  selector:
    app: portfolio-postgres
  ports:
    - name: db
      protocol: TCP
      port: 5432
      targetPort: 5432