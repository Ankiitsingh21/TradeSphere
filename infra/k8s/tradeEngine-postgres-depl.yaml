apiVersion: apps/v1
kind: Deployment
metadata:
  name: tradeengine-postgres-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tradeengine-postgres
  template:
    metadata:
      labels:
        app: tradeengine-postgres
    spec:
      containers:
        - name: tradeengine-postgres
          image: postgres
          env:
            - name: POSTGRES_DB
              value: tradeengine
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-tradeengine-srv
spec:
  selector:
    app: tradeengine-postgres
  ports:
    - name: db
      protocol: TCP
      port: 5432
      targetPort: 5432